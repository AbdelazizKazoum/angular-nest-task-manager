/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // enables toggling via a CSS class
  theme: {
    extend: {
      colors: {
        primary: '', // optional custom colors
      },
    },
  },
  plugins: [],
};
