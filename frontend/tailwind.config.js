/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // enables toggling via a CSS class
  theme: {
    extend: {
      colors: {
        primary: '#2563eb', // optional custom colors
      },
    },
  },
  plugins: [],
};
